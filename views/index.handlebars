<div class="article-section">
  {{#each articles}}
  <div class="row">
    <div class="col-lg-6" id="article-content">
      <h2> {{this.title}} </h2>
      <br>
      <p> {{this.body}}</p>
      <br>
      <p> {{this.link}}</p>
    </div>

    <button type="button" class="btn btn-primary" data-id={{this._id}} id="save-article" data-favorited={{this.favorited}}>Save Article</button>
    <button type="button" class="btn btn-primary" id='article-comment' data-id={{this._id}}> View Article Note </button>

    <div class="col-lg-5" id="comments">

    </div>
  </div>
  {{/each}}
</div>